[project]
name = "rat-storage-s3"
version = "0.2.0b1"
description = "S3 object-storage axis (storage/v1) for RAT's decoupled data architecture (ADR-024)"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.65",
    "protobuf>=5.27",
    "boto3>=1.35",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff==0.15.15", "pyright>=1.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rat_storage_s3"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "TCH"]

[tool.pyright]
typeCheckingMode = "strict"
pythonVersion = "3.12"
